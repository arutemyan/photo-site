# Composer
/vendor/
composer.lock

# データベース（実行時に自動作成されるため全体を除外）
/data/
/config/tests/
config/session_keys/

# 旧データベース場所（非推奨、実行時に自動作成）
/public/data/
/work/*.db

# アップロードファイル
/public/uploads/ogp/*
/public/uploads/images/*
/public/uploads/thumbs/*
/public/uploads/theme/*
/public/uploads/paintfiles/*
!public/uploads/ogp/.gitkeep
!public/uploads/images/.gitkeep
!public/uploads/thumbs/.gitkeep
!public/uploads/theme/.gitkeep
!public/uploads/paintfiles/.gitkeep

# キャッシュ（実行時に自動作成されるため全体を除外）
/cache/

# ログ（実行時に自動作成されるため全体を除外）
/logs/

# PHPUnit
/.phpunit.cache/
.phpunit.result.cache
/coverage/
/tests/reports/
/tests/tests
/tests/tmp_data

# IDE
/.idea/
/.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Node.js
/node_modules/
package-lock.json
pnpm-lock.yaml

# JavaScript bundles (ビルド生成物)
*.bundle.js
*.bundle.js.map

# CSS bundles (ビルド生成物)
*.bundle.css

# 環境設定
.env
.env.local
# ローカル設定ファイル（デフォルト設定を上書き）
config/*.local.php

# テスト用データベース
/test-cache/

# Temporary files
/tmp/
*.tmp

# リリースパッケージ
/releases/
*.tar.gz
*.zip

# ビルド時の一時ファイル
.git-archive-exclude
uploads/paintfiles/
workspace.code-workspace
GROUP_POSTS_*.md
GROUP_POSTS_QUICK_REF.txt
scripts/integration/
scripts/test_save_illust.php
