{
  "name": "photo-site",
  "version": "1.0.0",
  "description": "Build scripts for photo-site",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "build": "node build.js",
    "build:prod": "NODE_ENV=production node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
